user:
  name: Souradeep Mondal
  github: HBGauction

language: "python"
python_version: "3.10"

commands:
  build:
    - "pip install -r requirements.txt"
  start:
    - "python -m Grabber"

env:
  - "ENVIRONMENT=production"
